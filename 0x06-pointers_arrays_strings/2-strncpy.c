#include "main.h"
#include <stdio.h>
/**
 * function that copies a string
 */
char *_strncpy(char *dest, char *src, int n);
{
	char c = "dest";
        char d = "src\n";
        char *dest;
                dest = _strncpy(c, d);
        return (dest);
}

