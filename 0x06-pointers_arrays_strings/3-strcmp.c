#include "main.h"
#include <stdio.h>
/**
 * function that copies a string
 */
char *_strncpy(char *dest, char *sr
{
	char c = "dest";
        char d = "src";
        char *dest;
                dest = _strcmp(c, d);
        return (dest);
}
