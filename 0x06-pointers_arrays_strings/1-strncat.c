#include "main.h"
#include <stdio.h>

/**
 * function that concatenates two strings
 */
char *_strncat(char *dest, char *src, int n);
{
        char c = "dest";
        char d = "src\n";
        char *dest;

                dest = _strncat(c, d);
        return (dest);
}
	
