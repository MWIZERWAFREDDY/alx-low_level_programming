#include "main.h"

/**
 * a recursive function that prints a string ,followed by a new line
 */
void _puts_recursion(char *s);
{
	if (*s != '\0')
		_putchar(*s);
		_putchar('\n');
	return (0);

	void main(void);
{
	int b = _puts_recursions(s + 1);

	return (b);
}
