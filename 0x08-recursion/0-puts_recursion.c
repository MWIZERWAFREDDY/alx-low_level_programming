#include "main.h"

/**
 * a recursive function that prints a string ,followed by a new line
 */
void _puts_recursion(char *s);
{
	if (*s == '\0')
	{
		_putchar('\n');
		return;
	}

	void main(void);
{
	_putchar(*s);
	_puts_recursion(s + 1);

}
